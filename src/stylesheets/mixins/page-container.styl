@import '../settings'

container() {
  display: flex;
  flex-direction: column;

  align-items: center;

  .Container {
    //max-width: rupture['scale'][4];
    width: 100%;

    display: flex;
    flex-direction: column;

    align-items: center;
  }

  .Container-content {
    max-width: rupture['scale'][3];
    width: 100%;

    display: flex;
    flex-direction: column;

    align-items: center;
  }
}
